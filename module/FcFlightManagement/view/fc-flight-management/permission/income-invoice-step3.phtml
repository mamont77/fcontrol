<?php
$title = 'Check Permission income invoice';
$this->headTitle($title);
?>
<ul class="breadcrumb">
    <li><a href="/">Home</a> <span class="divider">/</span></li>
    <li>Management <span class="divider">/</span></li>
    <li>Permission income invoice</li>
</ul>

<h1><?= $title ?></h1>

<? if ($this->result): ?>
    <?=
    $this->partial('fc-flight-management/_widgets/forms/permission-income-invoice-step3.phtml',
        array(
            'data' => $this->result,
            'units' => $this->units,
            'currencies' => $this->currencies,
            'aircrafts' => $this->aircrafts,
            'typesOfPermission' => $this->typesOfPermission,
            'fixedHeader' => true,
        ))
    ?>
<? endif; ?>
