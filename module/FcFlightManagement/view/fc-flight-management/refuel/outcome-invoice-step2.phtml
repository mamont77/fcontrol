<?php
$title = 'Create refuel outcome invoice';
$this->headTitle($title);
?>
<ul class="breadcrumb">
    <li><a href="/">Home</a> <span class="divider">/</span></li>
    <li>Management <span class="divider">/</span></li>
    <li><a href="/management/refuel/outcome-invoice-step1">Refuel outcome invoice</a></li>
</ul>

<h1><?= $title ?></h1>

<? if ($this->result): ?>
    <?=
    $this->partial('fc-flight-management/_widgets/forms/refuel-outcome-invoice-step2.phtml',
        array(
            'newInvoiceNumber' => $this->newInvoiceNumber,
            'data' => $this->result,
            'units' => $this->units,
            'banks' => $this->banks,
            'currencies' => $this->currencies,
            'aircrafts' => $this->aircrafts,
            'fixedHeader' => true,
        ))
    ?>
<? endif; ?>
