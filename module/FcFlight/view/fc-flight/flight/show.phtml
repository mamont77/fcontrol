<ul class="breadcrumb">
    <li><a href="/">Home</a> <span class="divider">/</span></li>
    <?php
      if (($header->status)) {
          echo '<li><a href="/flights/active">In process</a> <span class="divider">/</span></li>';
      } else {
          echo '<li><a href="/flights/archived">Archived</a> <span class="divider">/</span></li>';
      }
    ?>
    <li class="active"><?= $header->refNumberOrder ?></li>
</ul>

<?php
/**
 * @var $this FcFlight\Controller\FlightController
 */
$title = 'Browse: Flight';
$this->headTitle($title);

?>
<legend><?php echo $this->escapeHtml($title); ?></legend>
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span2">
            <ul class="nav nav-list">
                <li class="nav-header">Operations</li>
                <li class="active">
                    <a href="#switchStatus" data-toggle="modal" role="button">
                        <i class="icon-refresh"></i> Change status</a>
                </li>
                <li class="<?= ($header->status) ? '' : 'disabled' ?>">

                    <a href="<?php echo $this->url('flight', array('action' => 'edit-header', 'id' => $header->id)); ?>">
                        <i class="icon-edit"></i> Edit
                    </a>
                </li>
                <li class="<?= ($header->status) ? '' : 'disabled' ?>">

                    <a href="<?php echo $this->url('flight', array('action' => 'delete-header', 'id' => $header->id)); ?>">
                        <i class="icon-remove"></i> Delete
                    </a>
                </li>
                <li class="divider"></li>
            </ul>
            <ul class="nav nav-list">
                <li class="nav-header">Add service</li>
                <li class="<?= ($header->status) ? '' : 'disabled' ?>">
                    <a href="<?php echo $this->url('leg', array('action' => 'add', 'id' => $header->id)); ?>">
                        <i class="icon-plane"></i> LEG</a>
                </li>
                <li class="<?= ($header->status) ? '' : 'disabled' ?>">
                    <a href="<?php echo $this->url('refuel', array('action' => 'add', 'id' => $header->id)); ?>">
                        <i class="icon-plane"></i> Refuel</a>
                </li>
                <li class="disabled"><a href="#">Permission</a></li>
                <li class="disabled"><a href="#">Hotel</a></li>
                <li class="disabled"><a href="#">FPL/PPL/SLOT</a></li>
                <li class="disabled"><a href="#">Charter</a></li>
                <li class="divider"></li>
            </ul>

            <div id="switchStatus" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="switchStatusLabel" aria-hidden="true">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                    <h3 id="switchStatusLabel">Change status warring</h3>
                </div>
                <div class="modal-body">
                    <p>ARE YOU COMPLETELY SURE TO CHANGE STATUS FOR FLIGHT<br /><b><?= $header->refNumberOrder ?></b>?</p>
                </div>
                <div class="modal-footer">
                    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
                    <a class="btn btn-primary" href="<?php echo $this->url('flight', array('action' => 'status-header', 'id' => $header->id)); ?>">
                        Change status</a>
                </div>
            </div>

        </div>
        <div class="span10">
            <?php echo $this->flashMessenger()->render('success', array('alert', 'alert-success')); ?>

            <div id="header">
                <table id="flightBrowse">
                    <tr>
                        <td>
                            <table class="cHeader table">
                                <tr>
                                    <th>Reg Number Order</th>
                                    <td class="cRefNumberOrder">
                                        <?php echo $this->escapeHtml($header->refNumberOrder); ?>
                                    </td>
                                </tr>
                                <tr>
                                    <th>Date Order</th>
                                    <td><?php echo date('Y-m-d', $header->dateOrder); ?></td>
                                </tr>
                                <tr>
                                    <th>Customer</th>
                                    <td><?php echo $this->escapeHtml($header->kontragentShortName); ?></td>
                                </tr>
                            </table>
                        </td>
                        <td>
                            <table class="cHeader table">
                                <tr>
                                    <th>Air Operator</th>
                                    <td><?php echo $this->escapeHtml($header->airOperatorShortName); ?></td>
                                </tr>
                                <tr>
                                    <th>Aircraft</th>
                                    <td><?php echo $this->escapeHtml($header->aircraftTypeName); ?>
                                        (<?php echo $this->escapeHtml($header->aircraft); ?>)
                                    </td>
                                </tr>
                                <tr class="<?= ($header->status) ? 'warning' : 'success'; ?>">
                                    <th>Status</th>
                                    <td><?= ($header->status) ? 'In Process' : 'Done'; ?></td>
                                </tr>
                            </table>
                        </td>
                    </tr>
                </table>
            </div>

            <? if (count($legs) > 0): ?>
                <?php echo $this->partial('widgets/tables/leg.phtml', array('legs' => $legs)); ?>
            <? endif; ?>

            <? if (count($refuels) > 0): ?>
                <?php echo $this->partial('widgets/tables/refuel.phtml', array('refuels' => $refuels)); ?>
            <? endif; ?>
        </div>
    </div>
</div>


